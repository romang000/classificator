version: '3.8'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: classificator
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      PGDATA: /data/postgres
    ports:
      - "5433:5432"
